---
import MySubmissions from '../components/client/MySubmissions';
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mis Certificados | CERTIA</title>
  <link rel="stylesheet" href="/src/styles/global.css">
</head>
<body class="bg-gray-50">
  
  <header class="bg-white shadow-sm border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 py-4 flex justify-between items-center">
      <div class="flex items-center gap-4">
        <a href="/dashboard" class="text-2xl font-bold text-gray-900 hover:text-primary-600 transition-colors">
          🎯 CERTIA
        </a>
        <span class="px-3 py-1 bg-green-100 text-green-800 rounded-full text-sm font-medium">
          👤 Cliente
        </span>
      </div>
      <div class="flex gap-3">
        <a href="/dashboard" class="px-4 py-2 bg-gray-200 text-gray-800 rounded-lg hover:bg-gray-300 transition-colors">
          ← Dashboard
        </a>
        <button
          onclick="handleLogout()"
          class="px-4 py-2 bg-red-100 text-red-700 rounded-lg hover:bg-red-200 transition-colors"
        >
          🚪 Cerrar Sesión
        </button>
      </div>
    </div>
  </header>

  <main class="max-w-7xl mx-auto px-4 py-8">
    <MySubmissions client:load />
  </main>

  <script>
    async function handleLogout() {
      const { signOut } = await import('../lib/supabase.ts');
      await signOut();
      window.location.href = '/login';
    }
    window.handleLogout = handleLogout;
  </script>

</body>
</html>